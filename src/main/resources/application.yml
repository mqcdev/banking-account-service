spring:
    application:
        name: banking-account-service
    config:
        import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:9002}
services:
    customer:
        service-name: banking-customer-service
